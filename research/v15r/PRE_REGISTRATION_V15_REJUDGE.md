# V15 健全データ再判定 事前登録（実行前に確定・変更禁止）

★本文書は再判定スクリプトの実行**前**にコミットする。閾値変更・セル探索・デシル切り直しの後付けは禁止。★

## 問い
「JRDBデータが生きていたら、V15（現行pkl・無変更）は勝てていたか」— 死因（データ死 vs 市場適応）の直接検証。

## 対象
- **6日**: 20260627 / 0628 / 0711 / 0802 / 0808 / 0809（JRDB死かつ feat_dump 存在、~215R）。
- 6/22-8/9 の他8開催日（7/4,7/5,7/12,7/18,7/19,7/25,7/26,8/1）は **feat_dump（当日出馬表スナップショット）が存在せず対象外**。JRDB単独での特徴再構築は v15r パイプライン（未実装）でありスコープ外。→ n の限界として明記。

## 比較アーム
| アーム | スコア | 特徴 |
|--------|--------|------|
| **D（劣化・実際）** | dump 収録の本番`スコア`（当時の実予測） | JRDB 40/40 死のまま |
| **H（健全化）** | `keiba_model_v15_central_live.pkl.gz`（本番live pkl・無変更）で再予測 | JRDB系特徴のみ再構築CSVから再計算・他（netkeiba/朝オッズ系）は当時の記録値そのまま |

## 買い目複製（台帳と同一ルール）
- 条件分類（predict_core: 頭数/距離/馬場 → A-E,X）→ スキップ連鎖（strategy_filters: 1000m以下/06特別/京都/C4/E/B）→ 三連複7点（TOP1軸-[T2,T3]-[T2..T6]、スコア上位6）/ 条件Eは馬連2点。100円/点。
- 配当 = `research/ruiji/raw_results/<date>.json` の確定 trio/umaren 払戻。

## ★校正ゲート（H の結果を読む前に必須）★
- アーム D の生成買い目を、台帳 `cumulative_results.csv` の 8/2 settled 34R の `trio_bets_str` と照合し、**一致率 ≥90%** であること。
- 未達なら複製ロジックのバグ → **H の結果を読まずに**複製を修正して再校正（これは判定基準の変更ではない）。

## 主要評価（事前固定）
1. **ROI_H**（総払戻/総投資）+ レースクラスタ bootstrap 95%CI（n=5000）。
2. **ペア差 ROI_H − ROI_D** + 同 bootstrap CI（同一レース集合・同一配当でスコアだけ入替）。

## 判定規則（事前固定）
| 結果 | 判定 |
|------|------|
| ROI_H 点推定 ≥100% **かつ** ペア差CI下限 > 0 | **「データ死が主因」支持** → V15復旧+T1v2 で再開路線（それでも paper 2-4週は必須） |
| ROI_H < 80% | **「市場適応」初実証** → v15r + 別施策路線 |
| その間 / ペア差CIが0跨ぎ | **非決定** → paper 延長で追加データ（再開はいずれにせよ paper 経由） |

## 感度分析（報告のみ・判定は変えない）
- 最高額 trio 配当1本を除外した ROI_H（ジャックポット依存の確認。2023年227%→174%の教訓）。
- 日別内訳・的中率。

## 限界（事前明記）
- n≈215R（賭け対象はスキップ後さらに減）は **確証に不足**（ペア差確証は t4≈3,500R 基準・6/11 Fable監査）。本判定は**路線選択**であり最終昇格判定ではない。
- H の netkeiba系特徴は当時の記録値＝「netkeiba も生きていた」世界線。JRDB のみ健全化した counterfactual。
- ZED系（jrdb_ze_*）は SED 再構築で更新されるが、6-8月の新走の一部は expanding 平均に遅延反映の可能性（軽微・過去のみ参照でリーク無し）。
