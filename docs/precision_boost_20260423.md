# 4/23 精度向上作業レポート

実施日時: 2026-04-23 13:35-14:15
ブランチ: fix/precision-boost-20260423
ベースHEAD: a7b244a5
本番: 2026-04-25 (土)

---

## 結論

| タスク | 結果 | 採用 |
|--------|------|------|
| 1. feature_lookups 最新化 | ソースCSV未更新で実質効果なし | ✗ 保留 |
| 2. 分布乖離チェック | 軽微 (大2/中5/小143)、修正不要 | ✓ ツール追加 |
| 3. 4/19 結果取得リトライ | 既settled済み、新規対象なし | ✓ 完了 |
| 4. cookie 確認 | 期限切れ、認証情報未保存 | △ 金曜手動 |
| 5. JRDB 結合軽微修正 | 直近a7b244a5で修正済み、リスク高 | ✗ 保留 |

**採用変更**: 1件（分布乖離チェックツール追加のみ）
**本番への影響**: 中立 (予測ロジック変更なし、レポート系のみ追加)

---

## タスク1: feature_lookups 最新化 — 保留

### 現状
- `data/feature_lookups.pkl` 最終更新 2026-03-27 (3/27 0:40)
- ソースCSV `data/jra_races_full.csv` も 2026-03-27 で停止
- CSV内最終データ: 2025-12-28

### 判断
ソースCSVが3/27から更新されていないため、`tools/precompute_lookups.py` を再実行しても出力は同一。再生成は無効。

### 来週以降のアクション
- jra_races_full.csv を 2026-04 まで更新する作業が必要 (TARGET JV からの抽出)
- これは別タスクとして切り出す

---

## タスク2: 分布乖離チェック — 軽微・修正不要

### 実装
- 新規: `tools/check_distribution_drift.py` (簡易ドリフト検出)
- 出力: `report/distribution_drift_20260423.md`

### 結果 (学習df 2024 vs 2025)
- 比較対象: 150/150 v15特徴量 (全カバー)
- 乖離大 (d > 0.3): **2個** — 要注意
- 乖離中 (0.1<d≤0.3): 5個
- 乖離小 (d≤0.1): 143個 (95.3%)

### 乖離大の特徴量
| 特徴量 | 2024 mean | 2025 mean | drift d | 解釈 |
|--------|-----------|-----------|---------|------|
| jrdb_rise_code | 2.722 | 3.013 | 0.422 | JRDB 上昇度コード分布変化 (zero率 0.10→0.00) |
| course_renovated | 0.051 | 0.000 | 0.327 | コース改装フラグの自然な減衰 |

### 判定
**軽微な要注意**: 2個のみで、いずれも自然な経時変化または非クリティカル特徴量。本番への影響は限定的。**修正なし**。

---

## タスク3: 4/19 結果取得リトライ — 完了

### 結果
- 4/19 全35Rは既settled済み (新規対象なし)
- 累計359R ROI **112.2%** (+30,650円)

### 条件別累計
| 条件 | N | ROI | 目標 |
|------|---|-----|------|
| A | 104 | 114.5% | 143.7% |
| B | 9 | 0.0% | 165.8% |
| C | 99 | 111.3% | 199.9% |
| D | 125 | 136.5% | 95.2% (達成) |
| E | 10 | 11.9% | 82.6% |
| X | 12 | 13.8% | 231.3% |

### アラート (要監視)
- 累計ROI 112.2% < 保守的142.6% (-21.3pt)
- A/Cが目標未達
- 直近30R ROI 44.0% (短期ドローダウン中)
- 現在2連敗

---

## タスク4: cookie 確認 — 金曜手動更新必要

### 結果
- `python tools/refresh_cookie.py --check` → NG: ログインセッション切れ
- `--auto` → 認証情報未保存のため自動更新不可

### 来週へのTODO
**金曜 (4/24) 昼に手動実行**:
```bash
python tools/refresh_cookie.py
```
※ Playwright対話式ログイン。netkeiba認証情報を入力。

---

## タスク5: JRDB 結合軽微修正 — 保留 (リスク高)

### 判断
- 直近a7b244a5 で SED merge NaN フォールバック修正済み (+15.7pt coverage)
- さらに正規化 (zfill/strip) を追加しても追加改善は限定的
- 破壊的変更リスクのほうが大きい
- 本番直前の修正は **凍結** が安全

### 来週以降
4/19の未結合ケース詳細分析 (TYB 0%/SED未結合分の原因究明) は別タスクで実施。

---

## 統合テスト

| 項目 | 結果 |
|------|------|
| 構文チェック (app.py, tools/check_distribution_drift.py) | PASS |
| 分布乖離スクリプト実行 | PASS |
| test_features.py | FAIL (既存バグ: app.py:6621 actual_roi key) — 私の変更外 |

### 既存バグ (本作業範囲外)
`app.py:6621` で `_ci['actual_roi']['trio']` 参照、`actual_roi_results.json` フォーマット変化が原因。
test 環境のみ影響、Streamlit実行時は別パスを通る可能性あり。次回タスクで対応。

---

## 本番準備状況 (4/25 土)

| 項目 | 状態 |
|------|------|
| モデル (v15_central_live) | OK 変更なし |
| 特徴量定義 | OK 変更なし |
| predict_core.py | OK 変更なし |
| JRDB merge | OK a7b244a5で修正済み |
| feature_lookups.pkl | OK (3/27、ソース古いが現状維持) |
| Cookie | **要更新 金曜昼** |
| タスクスケジューラ | OK 設定変更なし |
| 分布ドリフト | OK 軽微 |

### 最終判定
**本番予測精度への影響は中立**。Cookie 更新さえ忘れなければ、土曜本番は正常稼働見込み。

---

## 来週以降の TODO

1. jra_races_full.csv を 2026-04 まで更新 → feature_lookups 再生成
2. test_features.py の既存バグ修正 (app.py:6621 actual_roi key)
3. JRDB TYB/SED 未結合ケースの詳細分析
4. cookie の自動更新フロー整備 (認証情報の安全な保存)
